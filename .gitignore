# Environment files with secrets — never commit these
.env
frontend/.env
backend/.env

# Keep example files
!.env.example
!frontend/.env.example
!backend/.env.example
.claude/worktrees/
backups/

# Python bytecode cache (ai-engine, chatterbox-engine)
__pycache__/
*.pyc
